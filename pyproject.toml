[project]
name = "binaryshield-lifter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "capstone>=5.0.7",
    "icicle-emu>=0.0.11",
    "llvm-nanobind",
    "pefile>=2024.8.26",
]

[tool.uv.sources]
llvm-nanobind = { git = "https://github.com/LLVMParty/llvm-nanobind" }

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.34",
]
